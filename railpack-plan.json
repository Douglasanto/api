{
  "packages": {
    "java": "17.0.12",
    "maven": "3.9.11"
  },
  "build": {
    "cmds": [
      "export JAVA_HOME=$(mise where java) && export PATH=$JAVA_HOME/bin:$PATH && ./mvnw -DoutputFile=target/mvn-dependency-list.log -B -DskipTests clean dependency:list install -Pproduction"
    ]
  },
  "deploy": {
    "cmd": "java -Dserver.port=$PORT $JAVA_OPTS -jar target/*jar"
  }
}
